# 开发/测试依赖（与 requirements.txt 分离，仅用于本地测试与 CI）
# 安装：pip install -r requirements-dev.txt
# 该文件会自动拉取 requirements.txt 中的运行时依赖

-r requirements.txt

# ─── 测试框架 ──────────────────────────────────────────
pytest>=7.4,<9.0
pytest-asyncio>=0.23,<0.25     # 异用例测试（blasting_ws.py WebSocket / blasting.py async 路由）
pytest-cov>=4.1,<6.0           # 覆盖率报告（可选：pytest --cov=app）

# ─── HTTP / ASGI 测试客户端 ────────────────────────────
httpx>=0.26,<0.28               # FastAPI TestClient 底层依赖；直接用于路由集成测试

# ─── 代码质量（可选） ─────────────────────────────────
ruff>=0.5,<0.7                  # Linter / formatter（替代 flake8 + black）
